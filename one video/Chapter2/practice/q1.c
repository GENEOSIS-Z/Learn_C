#include <stdio.h>

int main()
{
    //Which of the following is invalid in C?
    // i. int a; b=a; // Invalid, the valid one is
    int a;
    int b = a;
    // ii. int v = 3^3; // valid bcz it doesnot gives error
    int v = 3 ^ 3;
    // iii. char dt = '21 dec 2020' Invalid valid one is
    char dt = '2' // supports only one character
        return 0;
}