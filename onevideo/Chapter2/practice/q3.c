#include <stdio.h>

int main()
{
    // Q3. Write a program to check whether the number is divisible by 97 or not?
    int a;
    printf("Enter the number\n");
    scanf("%d", &a);
    printf("Divisibility test returns : %d\n", a % 97);
    return 0;
}